CREATE TABLE USERS(ID INT PRIMARY KEY, USERNAME VARCHAR(45), PASSWORD VARCHAR(150), GRANTED_AUTHORITIES VARCHAR(350), EMAIL VARCHAR(60), NAME VARCHAR(60));
CREATE TABLE GRANTED_AUTHORITIES(ID VARCHAR(45) PRIMARY KEY, )